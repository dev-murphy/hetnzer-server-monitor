services:

  hetzner-monitor:
    build: .
    container_name: hetzner-monitor

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./config.yaml:/config/config.yaml:ro
      - ./data:/data